class Solution {
    public int[] solution(int[] lottos, int[] win_nums) {
        int zero = 0;
        int count = 0;
        int[] answer = new int[2];
        
        for (int i = 0; i < lottos.length; i++) {
            // 모르는 번호 개수 구하기
            if (lottos[i] == 0) {
                zero++;
                continue;
            }
            
